pipeline {
    // Agent -another remote machine
    //Jenkins has master - node

  agent any

 // tools {mvn 'maven-3.8.4 '}


  stages{
    stage ('checkout'){
        steps {
            checkout scm
       }
    }
  }
}